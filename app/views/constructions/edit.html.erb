<div class = "container">
  <div class = "col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
    <%=form_for(@construction, url: workplace_construction_path) do |f| %>
      <% if @construction.errors.any? %>
      <div id="error_explanation">
        <h2><%= @construction.errors.count %>件のエラーがあります。</h2>
        <ul>
        <% @construction.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <%= f.hidden_field :workplace_id %>
    <br>
    <br>
    <%= f.label :工事名 %><br />
    <br>
    <div class="field">
      <%= f.text_field :name,class: "form-control"  %>
    </div>
    <br>
      <%= f.submit "更新" %>
    <% end %>
  </div>
</div>
